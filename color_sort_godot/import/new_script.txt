@tool
extends EditorScript


func _run():
	var file = FileAccess.open("res://3_1.txt", FileAccess.READ)
	var num_bottles = file.get_64()
	var num_blocks = file.get_64()
	
	while file.eof_reached(): 
		var color_val = file.get_64()
		print(color_val)



		for i in arr:
			if i == 0:
				var num_bottles = arr[i]
				print("num_bottles = ", num_bottles)
			elif i == 1:
				var num_blocks = arr[i]
				print("num_blocks = ", num_blocks)
			else:
				color_val = arr[i]
				print("color val = ", color_val)


		var arr = file.get_csv_line()
		var vals = Array()
		for val in arr:
			vals.push_back(val)
		for i in range(2, vals.size):
			print(vals[i])